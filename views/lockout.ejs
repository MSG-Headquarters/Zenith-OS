<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Account Suspended | Zenith OS</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-void: #000000;
            --bg-card: #0a0f1a;
            --accent-rose: #F43F5E;
            --accent-amber: #F59E0B;
            --accent-emerald: #10B981;
            --text-primary: #F8FAFC;
            --text-secondary: #94A3B8;
            --text-muted: #64748B;
            --border: rgba(255, 255, 255, 0.1);
        }
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--bg-void);
            color: var(--text-primary);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
        }
        .bg-grid {
            position: fixed;
            top: 0; left: 0; right: 0; bottom: 0;
            background-image: 
                linear-gradient(rgba(244, 63, 94, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(244, 63, 94, 0.03) 1px, transparent 1px);
            background-size: 60px 60px;
            pointer-events: none;
        }
        .glow-orb { position: fixed; border-radius: 50%; filter: blur(100px); opacity: 0.3; pointer-events: none; }
        .orb-1 { width: 400px; height: 400px; background: var(--accent-rose); top: -150px; right: -100px; }
        .orb-2 { width: 300px; height: 300px; background: var(--accent-amber); bottom: -100px; left: -50px; }
        .lockout-container { position: relative; z-index: 10; width: 100%; max-width: 520px; padding: 20px; }
        .lockout-card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 24px;
            padding: 48px 40px;
            backdrop-filter: blur(20px);
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.5);
            text-align: center;
        }
        .lock-icon {
            width: 100px;
            height: 100px;
            background: rgba(244, 63, 94, 0.1);
            border: 2px solid rgba(244, 63, 94, 0.3);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 48px;
            margin: 0 auto 24px;
            animation: pulse 2s ease-in-out infinite;
        }
        .lock-icon.expired { background: rgba(245, 158, 11, 0.1); border-color: rgba(245, 158, 11, 0.3); }
        @keyframes pulse {
            0%, 100% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.05); opacity: 0.8; }
        }
        .lockout-title { font-size: 28px; font-weight: 800; margin-bottom: 12px; color: var(--accent-rose); }
        .lockout-title.expired { color: var(--accent-amber); }
        .lockout-subtitle { font-size: 16px; color: var(--text-secondary); margin-bottom: 32px; line-height: 1.6; }
        .info-box {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 24px;
            margin-bottom: 32px;
            text-align: left;
        }
        .info-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid var(--border); }
        .info-row:last-child { border-bottom: none; }
        .info-label { font-size: 13px; color: var(--text-muted); }
        .info-value { font-size: 13px; font-weight: 600; color: var(--text-primary); }
        .info-value.status-suspended { color: var(--accent-rose); }
        .info-value.status-expired { color: var(--accent-amber); }
        .reason-box {
            background: rgba(244, 63, 94, 0.1);
            border: 1px solid rgba(244, 63, 94, 0.2);
            border-radius: 12px;
            padding: 16px 20px;
            margin-bottom: 32px;
            text-align: left;
        }
        .reason-box.expired { background: rgba(245, 158, 11, 0.1); border-color: rgba(245, 158, 11, 0.2); }
        .reason-title { font-size: 12px; font-weight: 700; color: var(--accent-rose); text-transform: uppercase; letter-spacing: 1px; margin-bottom: 8px; }
        .reason-title.expired { color: var(--accent-amber); }
        .reason-text { font-size: 14px; color: var(--text-secondary); line-height: 1.6; }
        .action-buttons { display: flex; flex-direction: column; gap: 12px; }
        .btn {
            width: 100%;
            padding: 16px;
            border-radius: 12px;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
            border: none;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            text-decoration: none;
        }
        .btn-primary { background: linear-gradient(135deg, var(--accent-emerald), #059669); color: white; }
        .btn-primary:hover { transform: translateY(-2px); box-shadow: 0 8px 24px rgba(16, 185, 129, 0.3); }
        .btn-secondary { background: rgba(255, 255, 255, 0.05); border: 1px solid var(--border); color: var(--text-secondary); }
        .contact-info { margin-top: 32px; padding-top: 24px; border-top: 1px solid var(--border); }
        .contact-title { font-size: 11px; font-weight: 700; color: var(--text-muted); text-transform: uppercase; letter-spacing: 1px; margin-bottom: 16px; }
        .contact-methods { display: flex; justify-content: center; gap: 24px; }
        .contact-method { text-align: center; }
        .contact-method-icon { font-size: 24px; margin-bottom: 8px; }
        .contact-method-label { font-size: 11px; color: var(--text-muted); margin-bottom: 4px; }
        .contact-method-value { font-size: 13px; color: var(--text-primary); font-weight: 500; }
        .contact-method-value a { color: var(--accent-emerald); text-decoration: none; }
        .footer-text { margin-top: 24px; font-size: 11px; color: var(--text-muted); }
        .version-badge { position: fixed; bottom: 20px; right: 20px; font-size: 11px; color: var(--text-muted); }
    </style>
</head>
<body>
    <div class="bg-grid"></div>
    <div class="glow-orb orb-1"></div>
    <div class="glow-orb orb-2"></div>

    <div class="lockout-container">
        <div class="lockout-card">
            <% 
            const configs = {
                'suspended': {
                    icon: '🔒',
                    title: 'Account Suspended',
                    subtitle: 'Your Zenith OS license has been suspended. Access to all platform features is currently restricted.',
                    status: 'Suspended',
                    reasonTitle: 'Reason for Suspension',
                    reasonText: 'Payment overdue for more than 60 days. Please update your payment method to restore access.',
                    isExpired: false
                },
                'expired': {
                    icon: '⏰',
                    title: 'License Expired',
                    subtitle: 'Your Zenith OS license has expired. Renew now to continue accessing the platform.',
                    status: 'Expired',
                    reasonTitle: 'License Expiration',
                    reasonText: 'Your license period has ended. Contact Main Street Group to renew your subscription.',
                    isExpired: true
                },
                'terminated': {
                    icon: '⛔',
                    title: 'License Terminated',
                    subtitle: 'Your Zenith OS license has been permanently terminated.',
                    status: 'Terminated',
                    reasonTitle: 'Termination Notice',
                    reasonText: 'This license has been terminated due to a violation of the terms of service. All data will be deleted within 30 days.',
                    isExpired: false
                }
            };
            const config = configs[reason] || configs['suspended'];
            %>
            
            <div class="lock-icon <%= config.isExpired ? 'expired' : '' %>"><%= config.icon %></div>
            
            <h1 class="lockout-title <%= config.isExpired ? 'expired' : '' %>"><%= config.title %></h1>
            <p class="lockout-subtitle"><%= config.subtitle %></p>

            <div class="info-box">
                <div class="info-row">
                    <span class="info-label">Organization</span>
                    <span class="info-value"><%= typeof org !== 'undefined' && org ? org.name : 'Unknown' %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">License Status</span>
                    <span class="info-value <%= config.isExpired ? 'status-expired' : 'status-suspended' %>"><%= config.status %></span>
                </div>
                <div class="info-row">
                    <span class="info-label">Date</span>
                    <span class="info-value"><%= new Date().toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' }) %></span>
                </div>
            </div>

            <div class="reason-box <%= config.isExpired ? 'expired' : '' %>">
                <div class="reason-title <%= config.isExpired ? 'expired' : '' %>"><%= config.reasonTitle %></div>
                <p class="reason-text"><%= config.reasonText %></p>
            </div>

            <div class="action-buttons">
                <a href="https://umbrassi.com/billing" class="btn btn-primary">💳 Contact Support</a>
                <a href="/auth/login" class="btn btn-secondary">← Return to Login</a>
            </div>

            <div class="contact-info">
                <div class="contact-title">Need Help?</div>
                <div class="contact-methods">
                    <div class="contact-method">
                        <div class="contact-method-icon">📧</div>
                        <div class="contact-method-label">Email Support</div>
                        <div class="contact-method-value"><a href="mailto:support@umbrassi.com">support@umbrassi.com</a></div>
                    </div>
                    <div class="contact-method">
                        <div class="contact-method-icon">💬</div>
                        <div class="contact-method-label">Live Chat</div>
                        <div class="contact-method-value"><a href="https://umbrassi.com/support">umbrassi.com</a></div>
                    </div>
                </div>
            </div>

            <p class="footer-text">If you believe this is an error, please contact Main Street Group immediately.</p>
        </div>
    </div>

    <div class="version-badge">Zenith OS | License Enforcement System</div>
</body>
</html>
