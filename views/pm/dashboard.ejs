<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Property Management - Zenith OS</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: #1a1a2e; color: #fff; min-height: 100vh; display: flex; }
        .sidebar { width: 260px; background: #16213e; padding: 20px 0; display: flex; flex-direction: column; }
        .sidebar-header { padding: 0 20px 20px; border-bottom: 1px solid #1f3460; }
        .sidebar-header h2 { color: #4ade80; font-size: 1.25rem; }
        .sidebar-header p { color: #64748b; font-size: 0.8rem; }
        .nav-section { padding: 15px 0; }
        .nav-section-title { color: #64748b; font-size: 0.7rem; text-transform: uppercase; padding: 0 20px 10px; letter-spacing: 1px; }
        .nav-item { display: flex; align-items: center; gap: 12px; padding: 12px 20px; color: #94a3b8; text-decoration: none; transition: all 0.2s; }
        .nav-item:hover, .nav-item.active { background: #1f3460; color: #fff; }
        .nav-item.active { border-left: 3px solid #4ade80; }
        .nav-icon { font-size: 1.1rem; }
        .main-content { flex: 1; padding: 30px; overflow-y: auto; }
        .page-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 30px; }
        .page-header h1 { font-size: 1.75rem; }
        .page-header p { color: #64748b; }
        .stats-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(180px, 1fr)); gap: 20px; margin-bottom: 30px; }
        .stat-card { background: #16213e; border-radius: 12px; padding: 20px; }
        .stat-card.emergency { border-left: 4px solid #ef4444; }
        .stat-card.high { border-left: 4px solid #f97316; }
        .stat-card.new { border-left: 4px solid #3b82f6; }
        .stat-card.progress { border-left: 4px solid #a855f7; }
        .stat-card.complete { border-left: 4px solid #22c55e; }
        .stat-card.week { border-left: 4px solid #06b6d4; }
        .stat-value { font-size: 2rem; font-weight: 700; }
        .stat-label { color: #64748b; font-size: 0.85rem; }
        .card { background: #16213e; border-radius: 12px; padding: 20px; }
        .card-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
        .card-title { font-size: 1.1rem; font-weight: 600; }
        .filters { display: flex; gap: 10px; }
        .filter-select { background: #1a1a2e; border: 1px solid #1f3460; color: #fff; padding: 8px 12px; border-radius: 6px; }
        .btn { padding: 8px 16px; border-radius: 6px; border: none; cursor: pointer; font-weight: 500; }
        .btn-primary { background: #4ade80; color: #000; }
        table { width: 100%; border-collapse: collapse; }
        th, td { padding: 12px; text-align: left; border-bottom: 1px solid #1f3460; }
        th { color: #64748b; font-weight: 500; font-size: 0.8rem; text-transform: uppercase; }
        .badge { padding: 4px 8px; border-radius: 4px; font-size: 0.75rem; font-weight: 500; }
        .badge-emergency { background: #fecaca; color: #991b1b; }
        .badge-high { background: #fed7aa; color: #9a3412; }
        .badge-medium { background: #fef08a; color: #854d0e; }
        .badge-low { background: #bbf7d0; color: #166534; }
        .badge-new { background: #dbeafe; color: #1e40af; }
        .badge-progress { background: #e9d5ff; color: #6b21a8; }
        .badge-complete { background: #bbf7d0; color: #166534; }
        .empty-state { text-align: center; padding: 60px 20px; color: #64748b; }
        .sidebar-footer { margin-top: auto; padding: 20px; border-top: 1px solid #1f3460; }
        .user-info { display: flex; align-items: center; gap: 12px; }
        .user-avatar { width: 40px; height: 40px; background: #4ade80; border-radius: 50%; display: flex; align-items: center; justify-content: center; color: #000; font-weight: 600; }
        .user-name { font-weight: 500; }
        .user-role { color: #64748b; font-size: 0.8rem; }
    </style>
</head>
<body>
    <aside class="sidebar">
        <div class="sidebar-header">
            <h2>🏢 Zenith OS</h2>
            <p>Property Management</p>
        </div>
        <nav class="nav-section">
            <div class="nav-section-title">Dashboard</div>
            <a href="/dashboard" class="nav-item"><span class="nav-icon">🏠</span> AXIS Home</a>
        </nav>
        <nav class="nav-section">
            <div class="nav-section-title">Work Modules</div>
            <a href="/crm" class="nav-item"><span class="nav-icon">📊</span> CRM Pipeline</a>
            <a href="/huddle" class="nav-item"><span class="nav-icon">💬</span> The Huddle</a>
            <a href="/mailer" class="nav-item"><span class="nav-icon">📧</span> Mailer</a>
            <a href="/pm" class="nav-item active"><span class="nav-icon">🏢</span> Property Mgmt</a>
            <a href="/vault" class="nav-item"><span class="nav-icon">🔐</span> The Vault</a>
        </nav>
        <nav class="nav-section">
            <div class="nav-section-title">C-Suite Access</div>
            <a href="/command" class="nav-item"><span class="nav-icon">🎯</span> Command Center</a>
            <a href="/cfo" class="nav-item"><span class="nav-icon">💰</span> CFO Dashboard</a>
        </nav>
        <div class="sidebar-footer">
            <div class="user-info">
                <div class="user-avatar"><%= user.first_name ? user.first_name.charAt(0) : 'U' %></div>
                <div>
                    <div class="user-name"><%= user.first_name %> <%= user.last_name %></div>
                    <div class="user-role"><%= user.role %></div>
                </div>
            </div>
        </div>
    </aside>
    <main class="main-content">
        <div class="page-header">
            <div>
                <h1>🏢 Property Management</h1>
                <p style="color: var(--text-muted)">14 Managed Properties</p>
            </div>
            <a href="/maintenance" target="_blank" class="btn btn-primary">📝 Public Request Form</a>
        </div>
        <div class="stats-grid">
            <div class="stat-card emergency">
                <div class="stat-value" id="stat-emergency">0</div>
                <div class="stat-label">🚨 Emergency</div>
            </div>
            <div class="stat-card high">
                <div class="stat-value" id="stat-high">0</div>
                <div class="stat-label">⚠️ High Priority</div>
            </div>
            <div class="stat-card new">
                <div class="stat-value" id="stat-new">0</div>
                <div class="stat-label">🆕 New Requests</div>
            </div>
            <div class="stat-card progress">
                <div class="stat-value" id="stat-progress">0</div>
                <div class="stat-label">🔧 In Progress</div>
            </div>
            <div class="stat-card complete">
                <div class="stat-value" id="stat-complete">0</div>
                <div class="stat-label">✅ Completed</div>
            </div>
            <div class="stat-card week">
                <div class="stat-value" id="stat-week">0</div>
                <div class="stat-label">📅 This Week</div>
            </div>
        </div>
        <div class="card">
            <div class="card-header">
                <h3 class="card-title">Maintenance Requests</h3>
                <div class="filters">
                    <select class="filter-select" id="filter-status">
                        <option value="all">All Status</option>
                        <option value="New">New</option>
                        <option value="In Progress">In Progress</option>
                        <option value="Completed">Completed</option>
                    </select>
                    <select class="filter-select" id="filter-priority">
                        <option value="all">All Priority</option>
                        <option value="Emergency">Emergency</option>
                        <option value="High">High</option>
                        <option value="Medium">Medium</option>
                        <option value="Low">Low</option>
                    </select>
                    <button class="btn btn-primary" onclick="loadRequests()">Filter</button>
                </div>
            </div>
            <table>
                <thead>
                    <tr>
                        <th>Request ID</th>
                        <th>Property</th>
                        <th>Type</th>
                        <th>Priority</th>
                        <th>Status</th>
                        <th>Contact</th>
                        <th>Submitted</th>
                    </tr>
                </thead>
                <tbody id="requests-table">
                    <tr>
                        <td colspan="7" class="empty-state">
                            <div>📋 No maintenance requests yet</div>
                            <p>Requests submitted through the public form will appear here</p>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </main>
    <script>
        async function loadStats() {
            try {
                const res = await fetch('/api/pm/stats');
                const data = await res.json();
                if (data.success && data.stats) {
                    document.getElementById('stat-emergency').textContent = data.stats.emergency_count || 0;
                    document.getElementById('stat-high').textContent = data.stats.high_count || 0;
                    document.getElementById('stat-new').textContent = data.stats.new_count || 0;
                    document.getElementById('stat-progress').textContent = data.stats.in_progress_count || 0;
                    document.getElementById('stat-complete').textContent = data.stats.completed_count || 0;
                    document.getElementById('stat-week').textContent = data.stats.last_7_days || 0;
                }
            } catch (e) { console.error('Stats error:', e); }
        }
        async function loadRequests() {
            const status = document.getElementById('filter-status').value;
            const priority = document.getElementById('filter-priority').value;
            try {
                const res = await fetch('/