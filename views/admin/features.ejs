<!DOCTYPE html>
<html lang="en" data-theme="<%= typeof theme !== 'undefined' ? theme : 'light' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Feature Settings - CRE OS</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.2/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/theme.css">
    <script src="/js/theme.js"></script>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: #0f1724; color: #e2e8f0; min-height: 100vh; display: flex; }
        .sidebar { width: 240px; background: #1a2332; display: flex; flex-direction: column; border-right: 1px solid rgba(255,255,255,0.06); position: fixed; top: 0; bottom: 0; left: 0; z-index: 100; }
        .sidebar-header { padding: 20px 16px 16px; border-bottom: 1px solid rgba(255,255,255,0.06); }
        .sidebar-header h2 { font-size: 1.1rem; font-weight: 700; }
        .sidebar-header h2 span:first-child { color: #10b981; }
        .sidebar-header h2 span:last-child { color: #e2e8f0; }
        .sidebar-header p { color: #64748b; font-size: 0.75rem; margin-top: 2px; }
        .nav-section { padding: 12px 0; }
        .nav-section-label { color: #475569; font-size: 0.65rem; text-transform: uppercase; letter-spacing: 1.2px; padding: 0 16px 8px; font-weight: 600; }
        .nav-item { display: flex; align-items: center; gap: 10px; padding: 9px 16px; color: #94a3b8; text-decoration: none; font-size: 0.85rem; transition: all 0.15s; border-left: 3px solid transparent; }
        .nav-item:hover { background: rgba(255,255,255,0.04); color: #e2e8f0; }
        .nav-item.active { background: rgba(16, 185, 129, 0.08); color: #10b981; border-left-color: #10b981; }
        .nav-icon { font-size: 1rem; width: 20px; text-align: center; }
        .sidebar-footer { margin-top: auto; padding: 16px; border-top: 1px solid rgba(255,255,255,0.06); }
        .sidebar-footer .user-info { display: flex; align-items: center; gap: 10px; }
        .sidebar-footer .user-avatar { width: 32px; height: 32px; border-radius: 50%; background: #10b981; display: flex; align-items: center; justify-content: center; font-size: 0.75rem; font-weight: 600; color: #fff; }
        .sidebar-footer .user-name { font-size: 0.8rem; color: #e2e8f0; }
        .sidebar-footer .user-role { font-size: 0.65rem; color: #64748b; }
        .sidebar-footer a { color: #64748b; font-size: 0.75rem; text-decoration: none; display: block; margin-top: 8px; }
        .sidebar-footer a:hover { color: #ef4444; }
        .main-content { flex: 1; margin-left: 240px; min-height: 100vh; }
        .top-bar { display: flex; justify-content: space-between; align-items: center; padding: 16px 32px; border-bottom: 1px solid rgba(255,255,255,0.06); background: #1a2332; }
        .top-bar h1 { font-size: 1.35rem; font-weight: 700; }
        .top-bar h1 .accent { color: #f59e0b; }
        .admin-tabs { display: flex; gap: 4px; padding: 12px 32px 0; background: #1a2332; }
        .admin-tab { padding: 10px 20px; border-radius: 8px 8px 0 0; font-size: 0.85rem; font-weight: 500; color: #64748b; text-decoration: none; display: flex; align-items: center; gap: 8px; transition: all 0.15s; border: 1px solid transparent; border-bottom: none; }
        .admin-tab:hover { color: #e2e8f0; background: rgba(255,255,255,0.03); }
        .admin-tab.active { color: #f59e0b; background: #0f1724; border-color: rgba(255,255,255,0.06); }
        .content { padding: 24px 32px; }

        /* Alert messages */
        .alert { padding: 12px 16px; border-radius: 8px; margin-bottom: 20px; font-size: 0.85rem; display: flex; align-items: center; gap: 8px; }
        .alert-success { background: rgba(16,185,129,0.12); color: #10b981; border: 1px solid rgba(16,185,129,0.2); }
        .alert-error { background: rgba(239,68,68,0.12); color: #ef4444; border: 1px solid rgba(239,68,68,0.2); }

        /* Feature toggles grid */
        .features-header { margin-bottom: 24px; }
        .features-header h2 { font-size: 1.1rem; font-weight: 600; margin-bottom: 4px; }
        .features-header p { color: #64748b; font-size: 0.85rem; }

        .features-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); gap: 16px; margin-bottom: 28px; }

        .feature-card { background: #1a2332; border: 1px solid rgba(255,255,255,0.06); border-radius: 12px; padding: 20px; display: flex; align-items: center; gap: 16px; transition: all 0.2s; }
        .feature-card:hover { border-color: rgba(255,255,255,0.12); }
        .feature-card.disabled { opacity: 0.5; }

        .feature-icon { width: 44px; height: 44px; border-radius: 10px; display: flex; align-items: center; justify-content: center; font-size: 1.2rem; flex-shrink: 0; }
        .feature-icon.crm { background: rgba(59,130,246,0.15); color: #3b82f6; }
        .feature-icon.intel { background: rgba(245,158,11,0.15); color: #f59e0b; }
        .feature-icon.marketing { background: rgba(16,185,129,0.15); color: #10b981; }
        .feature-icon.huddle { background: rgba(139,92,246,0.15); color: #8b5cf6; }
        .feature-icon.vault { background: rgba(6,182,212,0.15); color: #06b6d4; }
        .feature-icon.cfo { background: rgba(236,72,153,0.15); color: #ec4899; }
        .feature-icon.danimal { background: rgba(34,197,94,0.15); color: #22c55e; }
        .feature-icon.inventory { background: rgba(249,115,22,0.15); color: #f97316; }
        .feature-icon.mailer { background: rgba(99,102,241,0.15); color: #6366f1; }
        .feature-icon.pm { background: rgba(168,85,247,0.15); color: #a855f7; }
        .feature-icon.command { background: rgba(239,68,68,0.15); color: #ef4444; }

        .feature-info { flex: 1; }
        .feature-name { font-weight: 600; font-size: 0.95rem; margin-bottom: 2px; }
        .feature-desc { color: #64748b; font-size: 0.78rem; line-height: 1.4; }

        /* Toggle switch */
        .toggle { position: relative; width: 48px; height: 26px; flex-shrink: 0; }
        .toggle input { opacity: 0; width: 0; height: 0; }
        .toggle-slider { position: absolute; cursor: pointer; top: 0; left: 0; right: 0; bottom: 0; background: #334155; border-radius: 26px; transition: 0.3s; }
        .toggle-slider:before { content: ''; position: absolute; height: 20px; width: 20px; left: 3px; bottom: 3px; background: #94a3b8; border-radius: 50%; transition: 0.3s; }
        .toggle input:checked + .toggle-slider { background: #10b981; }
        .toggle input:checked + .toggle-slider:before { transform: translateX(22px); background: #fff; }

        .section-divider { border: none; border-top: 1px solid rgba(255,255,255,0.06); margin: 8px 0 24px; }
        .section-label { font-size: 0.75rem; text-transform: uppercase; letter-spacing: 1px; color: #475569; font-weight: 600; margin-bottom: 16px; }

        .save-bar { display: flex; justify-content: space-between; align-items: center; padding: 16px 24px; background: #1a2332; border: 1px solid rgba(255,255,255,0.06); border-radius: 12px; }
        .save-bar p { color: #64748b; font-size: 0.8rem; }
        .btn-save { padding: 10px 28px; background: #10b981; color: #fff; border: none; border-radius: 8px; font-size: 0.9rem; font-weight: 600; cursor: pointer; transition: all 0.2s; display: flex; align-items: center; gap: 8px; }
        .btn-save:hover { background: #059669; }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <aside class="sidebar">
        <div class="sidebar-header">
            <h2><span>CRE</span> <span>OS</span></h2>
            <p><%= org?.name || 'Organization' %></p>
        </div>
        <div class="nav-section">
            <div class="nav-section-label">Navigation</div>
            <a href="/dashboard" class="nav-item"><i class="nav-icon bi bi-house"></i> Dashboard</a>
            <a href="/admin" class="nav-item"><i class="nav-icon bi bi-speedometer2"></i> Admin Home</a>
        </div>
        <div class="nav-section">
            <div class="nav-section-label">Administration</div>
            <a href="/admin/users" class="nav-item"><i class="nav-icon bi bi-people"></i> Users</a>
            <a href="/admin/roles" class="nav-item"><i class="nav-icon bi bi-shield"></i> Roles</a>
            <a href="/admin/features" class="nav-item active"><i class="nav-icon bi bi-toggles"></i> Features</a>
            <a href="/admin/audit" class="nav-item"><i class="nav-icon bi bi-clock-history"></i> Audit Log</a>
        </div>
        <div class="sidebar-footer">
            <div class="user-info">
                <div class="user-avatar"><%= user?.name ? user.name.charAt(0).toUpperCase() : '?' %></div>
                <div>
                    <div class="user-name"><%= user?.name || 'User' %></div>
                    <div class="user-role"><%= user?.role || 'Role' %></div>
                </div>
            </div>
            <a href="/auth/logout"><i class="bi bi-box-arrow-left"></i> Sign Out</a>
        </div>
    </aside>

    <!-- Main Content -->
    <div class="main-content">
        <div class="top-bar">
            <h1><i class="bi bi-gear"></i> <span class="accent">Admin</span> Settings</h1>
        </div>

        <!-- Admin Tabs -->
        <div class="admin-tabs">
            <a href="/admin" class="admin-tab"><i class="bi bi-speedometer2"></i> Overview</a>
            <a href="/admin/users" class="admin-tab"><i class="bi bi-people"></i> Users</a>
            <a href="/admin/roles" class="admin-tab"><i class="bi bi-shield"></i> Roles</a>
            <a href="/admin/features" class="admin-tab active"><i class="bi bi-toggles"></i> Features</a>
            <a href="/admin/audit" class="admin-tab"><i class="bi bi-clock-history"></i> Audit</a>
        </div>

        <div class="content">
            <% if (typeof success !== 'undefined' || (typeof query !== 'undefined' && query?.success)) { %>
            <div class="alert alert-success"><i class="bi bi-check-circle"></i> <%= typeof success !== 'undefined' ? success : query.success %></div>
            <% } %>
            <% if (typeof error !== 'undefined' || (typeof query !== 'undefined' && query?.error)) { %>
            <div class="alert alert-error"><i class="bi bi-exclamation-circle"></i> <%= typeof error !== 'undefined' ? error : query.error %></div>
            <% } %>

            <div class="features-header">
                <h2><i class="bi bi-toggles" style="color:#f59e0b;"></i> Module Feature Toggles</h2>
                <p>Control which modules appear in the sidebar for all users in your organization. Disabled modules are hidden but data is preserved.</p>
            </div>

            <form method="POST" action="/admin/features">

                <div class="section-label">Core Work Modules</div>
                <div class="features-grid">
                    <div class="feature-card">
                        <div class="feature-icon crm"><i class="bi bi-kanban"></i></div>
                        <div class="feature-info">
                            <div class="feature-name">CRM Pipeline</div>
                            <div class="feature-desc">Lead tracking, deal pipeline, and contact management</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" name="crm" <% if (features.crm !== false) { %>checked<% } %>>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>

                    <div class="feature-card">
                        <div class="feature-icon intel"><i class="bi bi-lightbulb"></i></div>
                        <div class="feature-info">
                            <div class="feature-name">INTEL</div>
                            <div class="feature-desc">AI market research and property intelligence</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" name="intel" <% if (features.intel !== false) { %>checked<% } %>>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>

                    <div class="feature-card">
                        <div class="feature-icon marketing"><i class="bi bi-megaphone"></i></div>
                        <div class="feature-info">
                            <div class="feature-name">Marketing Suite</div>
                            <div class="feature-desc">AI-generated marketing flyers and distribution</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" name="marketing" <% if (features.marketing !== false) { %>checked<% } %>>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>

                    <div class="feature-card">
                        <div class="feature-icon inventory"><i class="bi bi-box-seam"></i></div>
                        <div class="feature-info">
                            <div class="feature-name">Inventory</div>
                            <div class="feature-desc">Property listings and inventory management</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" name="inventory" <% if (features.inventory !== false) { %>checked<% } %>>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>
                </div>

                <div class="section-label">Communication & Collaboration</div>
                <div class="features-grid">
                    <div class="feature-card">
                        <div class="feature-icon huddle"><i class="bi bi-chat-dots"></i></div>
                        <div class="feature-info">
                            <div class="feature-name">The Huddle</div>
                            <div class="feature-desc">Team messaging, channels, and direct messages</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" name="huddle" <% if (features.huddle !== false) { %>checked<% } %>>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>

                    <div class="feature-card">
                        <div class="feature-icon mailer"><i class="bi bi-envelope"></i></div>
                        <div class="feature-info">
                            <div class="feature-name">Mailer</div>
                            <div class="feature-desc">Email campaigns and automated communications</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" name="mailer" <% if (features.mailer !== false) { %>checked<% } %>>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>

                    <div class="feature-card">
                        <div class="feature-icon vault"><i class="bi bi-safe"></i></div>
                        <div class="feature-info">
                            <div class="feature-name">The Vault</div>
                            <div class="feature-desc">Secure document storage and sharing</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" name="vault" <% if (features.vault !== false) { %>checked<% } %>>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>
                </div>

                <div class="section-label">Data & Analytics</div>
                <div class="features-grid">
                    <div class="feature-card">
                        <div class="feature-icon danimal"><i class="bi bi-database"></i></div>
                        <div class="feature-info">
                            <div class="feature-name">Danimal Data</div>
                            <div class="feature-desc">Florida property records and business intelligence</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" name="danimal" <% if (features.danimal !== false) { %>checked<% } %>>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>

                    <div class="feature-card">
                        <div class="feature-icon pm"><i class="bi bi-building"></i></div>
                        <div class="feature-info">
                            <div class="feature-name">Property Management</div>
                            <div class="feature-desc">Property operations and tenant management</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" name="pm" <% if (features.pm !== false) { %>checked<% } %>>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>
                </div>

                <div class="section-label">Executive Access</div>
                <div class="features-grid">
                    <div class="feature-card">
                        <div class="feature-icon cfo"><i class="bi bi-graph-up"></i></div>
                        <div class="feature-info">
                            <div class="feature-name">CFO Dashboard</div>
                            <div class="feature-desc">Financial analytics and reporting</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" name="cfo" <% if (features.cfo !== false) { %>checked<% } %>>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>

                    <div class="feature-card">
                        <div class="feature-icon command"><i class="bi bi-bullseye"></i></div>
                        <div class="feature-info">
                            <div class="feature-name">Command Center</div>
                            <div class="feature-desc">Executive overview and portfolio analytics</div>
                        </div>
                        <label class="toggle">
                            <input type="checkbox" name="command" <% if (features.command !== false) { %>checked<% } %>>
                            <span class="toggle-slider"></span>
                        </label>
                    </div>
                </div>

                <hr class="section-divider">

                <div class="save-bar">
                    <p><i class="bi bi-info-circle"></i> Changes take effect immediately for all users after their next page load. No data is deleted when modules are hidden.</p>
                    <button type="submit" class="btn-save"><i class="bi bi-check-lg"></i> Save Changes</button>
                </div>
            </form>
        </div>
    </div>

<script>
    // Handle success/error query params
    const params = new URLSearchParams(window.location.search);
    if (params.get('success')) {
        document.querySelector('.alert-success')?.scrollIntoView({ behavior: 'smooth' });
    }

    // Live toggle visual feedback
    document.querySelectorAll('.toggle input').forEach(toggle => {
        toggle.addEventListener('change', function() {
            this.closest('.feature-card').classList.toggle('disabled', !this.checked);
        });
        // Set initial state
        if (!toggle.checked) toggle.closest('.feature-card').classList.add('disabled');
    });
</script>
</body>
</html>
