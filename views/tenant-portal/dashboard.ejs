<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tenant Portal | <%= tenant.propertyName %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        :root {
            --bg-primary: #0f172a;
            --bg-card: #1e293b;
            --bg-card-hover: #263548;
            --border: rgba(255,255,255,0.08);
            --text-primary: #f1f5f9;
            --text-secondary: #94a3b8;
            --text-muted: #64748b;
            --accent-blue: #3b82f6;
            --accent-green: #10b981;
            --accent-amber: #f59e0b;
            --accent-red: #ef4444;
            --accent-purple: #8b5cf6;
        }
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: var(--bg-primary);
            color: var(--text-primary);
            min-height: 100vh;
        }

        /* ─── Top Bar ─── */
        .topbar {
            background: var(--bg-card);
            border-bottom: 1px solid var(--border);
            padding: 0 24px;
            height: 60px;
            display: flex;
            align-items: center;
            justify-content: space-between;
            position: sticky;
            top: 0;
            z-index: 100;
        }
        .topbar-left { display: flex; align-items: center; gap: 12px; }
        .topbar-logo {
            width: 36px; height: 36px;
            background: linear-gradient(135deg, #3b82f6, #06b6d4);
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .topbar-logo i { color: white; font-size: 18px; }
        .topbar-title { font-size: 16px; font-weight: 700; color: var(--text-primary); }
        .topbar-subtitle { font-size: 12px; color: var(--text-muted); }
        .topbar-right { display: flex; align-items: center; gap: 16px; }
        .tenant-badge {
            background: rgba(59,130,246,0.1);
            border: 1px solid rgba(59,130,246,0.2);
            border-radius: 8px;
            padding: 6px 12px;
            font-size: 13px;
            color: #93c5fd;
        }
        .btn-logout {
            background: transparent;
            border: 1px solid rgba(255,255,255,0.1);
            color: var(--text-muted);
            padding: 6px 14px;
            border-radius: 8px;
            font-size: 13px;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.2s;
        }
        .btn-logout:hover { color: var(--text-primary); border-color: rgba(255,255,255,0.2); }

        /* ─── Navigation ─── */
        .portal-nav {
            background: var(--bg-card);
            border-bottom: 1px solid var(--border);
            padding: 0 24px;
            display: flex;
            gap: 0;
            overflow-x: auto;
        }
        .nav-tab {
            padding: 14px 20px;
            color: var(--text-muted);
            text-decoration: none;
            font-size: 14px;
            font-weight: 500;
            border-bottom: 2px solid transparent;
            white-space: nowrap;
            transition: all 0.2s;
        }
        .nav-tab:hover { color: var(--text-secondary); }
        .nav-tab.active { color: var(--accent-blue); border-bottom-color: var(--accent-blue); }
        .nav-tab i { margin-right: 6px; }

        /* ─── Content ─── */
        .content { padding: 24px; max-width: 1200px; margin: 0 auto; }
        .welcome-banner {
            background: linear-gradient(135deg, rgba(59,130,246,0.15), rgba(6,182,212,0.1));
            border: 1px solid rgba(59,130,246,0.2);
            border-radius: 14px;
            padding: 24px;
            margin-bottom: 24px;
        }
        .welcome-banner h2 { font-size: 20px; margin-bottom: 4px; }
        .welcome-banner p { color: var(--text-secondary); font-size: 14px; }

        /* ─── Section Grid ─── */
        .section-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }
        @media (max-width: 768px) { .section-grid { grid-template-columns: 1fr; } }

        .section-card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 14px;
            overflow: hidden;
        }
        .section-card.full-width { grid-column: 1 / -1; }
        .section-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 16px 20px;
            border-bottom: 1px solid var(--border);
        }
        .section-header h3 { font-size: 15px; font-weight: 600; display: flex; align-items: center; gap: 8px; }
        .section-header .badge {
            background: rgba(59,130,246,0.15);
            color: #93c5fd;
            padding: 2px 8px;
            border-radius: 10px;
            font-size: 12px;
            font-weight: 600;
        }
        .section-header a {
            color: var(--accent-blue);
            text-decoration: none;
            font-size: 13px;
            font-weight: 500;
        }
        .section-body { padding: 16px 20px; }

        /* ─── Notification Items ─── */
        .notification-item {
            display: flex;
            gap: 12px;
            padding: 12px 0;
            border-bottom: 1px solid var(--border);
        }
        .notification-item:last-child { border-bottom: none; }
        .notif-icon {
            width: 38px; height: 38px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-shrink: 0;
            font-size: 16px;
        }
        .notif-icon.construction { background: rgba(245,158,11,0.15); color: var(--accent-amber); }
        .notif-icon.maintenance { background: rgba(59,130,246,0.15); color: var(--accent-blue); }
        .notif-icon.safety { background: rgba(239,68,68,0.15); color: var(--accent-red); }
        .notif-icon.event { background: rgba(139,92,246,0.15); color: var(--accent-purple); }
        .notif-icon.general { background: rgba(148,163,184,0.15); color: var(--text-secondary); }
        .notif-content { flex: 1; min-width: 0; }
        .notif-title { font-size: 14px; font-weight: 600; margin-bottom: 2px; }
        .notif-body { font-size: 13px; color: var(--text-secondary); line-height: 1.4; 
            display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; overflow: hidden; }
        .notif-meta { font-size: 11px; color: var(--text-muted); margin-top: 4px; }
        .priority-urgent { border-left: 3px solid var(--accent-red); padding-left: 12px; }
        .priority-high { border-left: 3px solid var(--accent-amber); padding-left: 12px; }

        /* ─── Video Card ─── */
        .video-card { padding: 12px 0; border-bottom: 1px solid var(--border); }
        .video-card:last-child { border-bottom: none; }
        .video-embed {
            position: relative;
            padding-bottom: 56.25%;
            border-radius: 10px;
            overflow: hidden;
            margin-bottom: 10px;
            background: #000;
        }
        .video-embed iframe {
            position: absolute;
            top: 0; left: 0;
            width: 100%; height: 100%;
            border: none;
        }
        .video-title { font-size: 14px; font-weight: 600; margin-bottom: 2px; }
        .video-date { font-size: 12px; color: var(--text-muted); }
        .video-desc { font-size: 13px; color: var(--text-secondary); margin-top: 4px; line-height: 1.4; }

        /* ─── Maintenance Items ─── */
        .maint-item {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 12px 0;
            border-bottom: 1px solid var(--border);
        }
        .maint-item:last-child { border-bottom: none; }
        .maint-status {
            padding: 3px 10px;
            border-radius: 6px;
            font-size: 11px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.3px;
            flex-shrink: 0;
        }
        .status-new { background: rgba(59,130,246,0.15); color: #93c5fd; }
        .status-assigned, .status-in-progress { background: rgba(245,158,11,0.15); color: #fcd34d; }
        .status-completed { background: rgba(16,185,129,0.15); color: #6ee7b7; }
        .status-closed { background: rgba(148,163,184,0.15); color: var(--text-muted); }
        .maint-info { flex: 1; min-width: 0; }
        .maint-type { font-size: 14px; font-weight: 600; }
        .maint-desc { font-size: 13px; color: var(--text-secondary);
            white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
        .maint-date { font-size: 12px; color: var(--text-muted); }
        .maint-id { font-size: 11px; color: var(--text-muted); font-family: monospace; }

        .empty-state {
            text-align: center;
            padding: 30px 20px;
            color: var(--text-muted);
        }
        .empty-state i { font-size: 32px; margin-bottom: 8px; display: block; opacity: 0.5; }
        .empty-state p { font-size: 13px; }

        .btn-new-request {
            display: inline-flex;
            align-items: center;
            gap: 6px;
            padding: 8px 16px;
            background: linear-gradient(135deg, #10b981, #059669);
            border: none;
            border-radius: 8px;
            color: white;
            font-size: 13px;
            font-weight: 600;
            cursor: pointer;
            text-decoration: none;
            transition: all 0.2s;
        }
        .btn-new-request:hover { transform: translateY(-1px); box-shadow: 0 4px 15px rgba(16,185,129,0.3); }

        /* ─── Stat Cards ─── */
        .stat-row {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 12px;
            margin-bottom: 24px;
        }
        @media (max-width: 640px) { .stat-row { grid-template-columns: 1fr; } }
        .stat-card {
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 16px;
            text-align: center;
        }
        .stat-card .value { font-size: 28px; font-weight: 700; }
        .stat-card .label { font-size: 12px; color: var(--text-muted); margin-top: 2px; }
        .stat-blue .value { color: var(--accent-blue); }
        .stat-green .value { color: var(--accent-green); }
        .stat-amber .value { color: var(--accent-amber); }

        @media (max-width: 640px) {
            .topbar { padding: 0 16px; }
            .tenant-badge { display: none; }
            .content { padding: 16px; }
            .portal-nav { padding: 0 16px; }
        }
    </style>
</head>
<body>
    <!-- Top Bar -->
    <div class="topbar">
        <div class="topbar-left">
            <div class="topbar-logo"><i class="bi bi-building"></i></div>
            <div>
                <div class="topbar-title"><%= tenant.propertyName %></div>
                <div class="topbar-subtitle">Tenant Portal</div>
            </div>
        </div>
        <div class="topbar-right">
            <span class="tenant-badge"><i class="bi bi-person-circle"></i> <%= tenant.dbaName || tenant.tenantName %> &bull; Unit <%= tenant.unitNumber %></span>
            <a href="/tenant-portal/logout" class="btn-logout"><i class="bi bi-box-arrow-right"></i> Logout</a>
        </div>
    </div>

    <!-- Navigation -->
    <nav class="portal-nav">
        <a href="/tenant-portal" class="nav-tab active"><i class="bi bi-house"></i> Dashboard</a>
        <a href="/tenant-portal/maintenance" class="nav-tab"><i class="bi bi-wrench"></i> Maintenance</a>
        <a href="/tenant-portal/mitch-minutes" class="nav-tab"><i class="bi bi-camera-video"></i> Mitch Minutes</a>
        <a href="/tenant-portal/updates" class="nav-tab"><i class="bi bi-bell"></i> Updates</a>
    </nav>

    <!-- Content -->
    <div class="content">
        <!-- Welcome Banner -->
        <div class="welcome-banner">
            <h2>Welcome, <%= (tenant.dbaName || tenant.tenantName).split(',')[0].split(' ')[0] %></h2>
            <p><%= tenant.propertyAddress %> &bull; Unit <%= tenant.unitNumber %></p>
        </div>

        <!-- Stat Cards -->
        <div class="stat-row">
            <div class="stat-card stat-amber">
                <div class="value"><%= openRequests %></div>
                <div class="label">Open Requests</div>
            </div>
            <div class="stat-card stat-blue">
                <div class="value"><%= updates.length %></div>
                <div class="label">Active Updates</div>
            </div>
            <div class="stat-card stat-green">
                <div class="value"><%= videos.length %></div>
                <div class="label">Mitch Minutes</div>
            </div>
        </div>

        <!-- Two Column Grid -->
        <div class="section-grid">

            <!-- Property Updates -->
            <div class="section-card">
                <div class="section-header">
                    <h3><i class="bi bi-bell-fill" style="color: var(--accent-amber)"></i> Property Updates</h3>
                    <a href="/tenant-portal/updates">View All</a>
                </div>
                <div class="section-body">
                    <% if (updates.length === 0) { %>
                        <div class="empty-state">
                            <i class="bi bi-check-circle"></i>
                            <p>No active updates for your property</p>
                        </div>
                    <% } else { %>
                        <% updates.slice(0, 4).forEach(u => { %>
                            <div class="notification-item <%= u.priority === 'urgent' ? 'priority-urgent' : (u.priority === 'high' ? 'priority-high' : '') %>">
                                <div class="notif-icon <%= u.category %>">
                                    <i class="bi bi-<%= u.category === 'construction' ? 'cone-striped' : 
                                        (u.category === 'maintenance' ? 'tools' : 
                                        (u.category === 'safety' ? 'shield-exclamation' : 
                                        (u.category === 'event' ? 'calendar-event' : 'info-circle'))) %>"></i>
                                </div>
                                <div class="notif-content">
                                    <div class="notif-title"><%= u.title %></div>
                                    <div class="notif-body"><%= u.body %></div>
                                    <div class="notif-meta">
                                        <%= new Date(u.created_at).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %>
                                        <% if (u.starts_at) { %>
                                            &bull; Starts <%= new Date(u.starts_at).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %>
                                        <% } %>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    <% } %>
                </div>
            </div>

            <!-- Maintenance Requests -->
            <div class="section-card">
                <div class="section-header">
                    <h3><i class="bi bi-wrench" style="color: var(--accent-blue)"></i> Maintenance</h3>
                    <a href="/tenant-portal/maintenance" class="btn-new-request"><i class="bi bi-plus-circle"></i> New Request</a>
                </div>
                <div class="section-body">
                    <% if (requests.length === 0) { %>
                        <div class="empty-state">
                            <i class="bi bi-check-circle"></i>
                            <p>No maintenance requests</p>
                        </div>
                    <% } else { %>
                        <% requests.slice(0, 5).forEach(r => { 
                            const statusClass = r.status === 'New' ? 'status-new' : 
                                (r.status === 'Completed' ? 'status-completed' : 
                                (r.status === 'Closed' ? 'status-closed' : 'status-in-progress'));
                        %>
                            <div class="maint-item">
                                <span class="maint-status <%= statusClass %>"><%= r.status %></span>
                                <div class="maint-info">
                                    <div class="maint-type"><%= r.request_type %></div>
                                    <div class="maint-desc"><%= r.description %></div>
                                </div>
                                <div style="text-align: right; flex-shrink: 0;">
                                    <div class="maint-id"><%= r.request_id %></div>
                                    <div class="maint-date"><%= new Date(r.submitted_at).toLocaleDateString('en-US', { month: 'short', day: 'numeric' }) %></div>
                                </div>
                            </div>
                        <% }); %>
                    <% } %>
                </div>
            </div>

            <!-- Mitch Minutes -->
            <div class="section-card full-width">
                <div class="section-header">
                    <h3><i class="bi bi-camera-video-fill" style="color: var(--accent-green)"></i> Mitch Minutes</h3>
                    <a href="/tenant-portal/mitch-minutes">View All Episodes</a>
                </div>
                <div class="section-body">
                    <% if (videos.length === 0) { %>
                        <div class="empty-state">
                            <i class="bi bi-camera-video-off"></i>
                            <p>No videos available yet. Check back soon!</p>
                        </div>
                    <% } else { %>
                        <div style="display: grid; grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); gap: 16px;">
                            <% videos.forEach((v, i) => { 
                                // Extract YouTube ID if applicable
                                let embedUrl = v.video_url;
                                const ytMatch = v.video_url.match(/(?:youtube\.com\/watch\?v=|youtu\.be\/|youtube\.com\/embed\/)([^&\s]+)/);
                                if (ytMatch) embedUrl = 'https://www.youtube.com/embed/' + ytMatch[1];
                                const vimeoMatch = v.video_url.match(/vimeo\.com\/(\d+)/);
                                if (vimeoMatch) embedUrl = 'https://player.vimeo.com/video/' + vimeoMatch[1];
                            %>
                                <div class="video-card">
                                    <div class="video-embed">
                                        <iframe src="<%= embedUrl %>" allowfullscreen loading="lazy"></iframe>
                                    </div>
                                    <div class="video-title"><%= v.title %></div>
                                    <div class="video-date"><%= new Date(v.published_at).toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) %>
                                        <% if (v.duration_seconds) { %>
                                            &bull; <%= Math.floor(v.duration_seconds / 60) %>:<%= String(v.duration_seconds % 60).padStart(2, '0') %>
                                        <% } %>
                                    </div>
                                    <% if (v.description) { %><div class="video-desc"><%= v.description %></div><% } %>
                                </div>
                            <% }); %>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
