<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>INTEL Research - Zenith OS</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: #0f1724; color: #e2e8f0; min-height: 100vh; display: flex; }

        .sidebar { width: 240px; background: #1a2332; display: flex; flex-direction: column; border-right: 1px solid rgba(255,255,255,0.06); position: fixed; top: 0; bottom: 0; left: 0; z-index: 100; }
        .sidebar-header { padding: 20px 16px 16px; border-bottom: 1px solid rgba(255,255,255,0.06); }
        .sidebar-header h2 { font-size: 1.1rem; font-weight: 700; }
        .sidebar-header h2 span:first-child { color: #10b981; }
        .sidebar-header h2 span:last-child { color: #e2e8f0; }
        .sidebar-header p { color: #64748b; font-size: 0.75rem; margin-top: 2px; }
        .nav-section { padding: 12px 0; }
        .nav-section-label { color: #475569; font-size: 0.65rem; text-transform: uppercase; letter-spacing: 1.2px; padding: 0 16px 8px; font-weight: 600; }
        .nav-item { display: flex; align-items: center; gap: 10px; padding: 9px 16px; color: #94a3b8; text-decoration: none; font-size: 0.85rem; transition: all 0.15s; border-left: 3px solid transparent; }
        .nav-item:hover { background: rgba(255,255,255,0.04); color: #e2e8f0; }
        .nav-item.active { background: rgba(16, 185, 129, 0.08); color: #10b981; border-left-color: #10b981; }
        .nav-icon { font-size: 1rem; width: 20px; text-align: center; }
        .sidebar-footer { margin-top: auto; padding: 16px; border-top: 1px solid rgba(255,255,255,0.06); }
        .sidebar-footer .user-info { display: flex; align-items: center; gap: 10px; }
        .sidebar-footer .user-avatar { width: 32px; height: 32px; border-radius: 50%; background: #10b981; display: flex; align-items: center; justify-content: center; font-size: 0.75rem; font-weight: 600; color: #fff; }
        .sidebar-footer .user-name { font-size: 0.8rem; color: #e2e8f0; }
        .sidebar-footer .user-role { font-size: 0.65rem; color: #64748b; }
        .sidebar-footer a { color: #64748b; font-size: 0.75rem; text-decoration: none; display: block; margin-top: 8px; }
        .sidebar-footer a:hover { color: #ef4444; }

        .main-content { flex: 1; margin-left: 240px; min-height: 100vh; }

        .top-bar { display: flex; justify-content: space-between; align-items: center; padding: 16px 32px; border-bottom: 1px solid rgba(255,255,255,0.06); background: #1a2332; }
        .top-bar h1 { font-size: 1.35rem; font-weight: 700; }
        .top-bar h1 .accent { color: #10b981; }
        .top-bar-actions { display: flex; gap: 10px; }
        .btn { padding: 8px 16px; border-radius: 8px; font-size: 0.85rem; font-weight: 500; border: none; cursor: pointer; text-decoration: none; display: inline-flex; align-items: center; gap: 6px; transition: all 0.15s; }
        .btn-ghost { background: transparent; color: #94a3b8; border: 1px solid rgba(255,255,255,0.1); }
        .btn-ghost:hover { background: rgba(255,255,255,0.05); color: #e2e8f0; }
        .btn-primary { background: #10b981; color: #fff; }
        .btn-primary:hover { background: #059669; }
        .btn-blue { background: #3b82f6; color: #fff; }
        .btn-blue:hover { background: #2563eb; }

        .intel-tabs { display: flex; gap: 4px; padding: 12px 32px 0; background: #1a2332; }
        .intel-tab { padding: 10px 20px; border-radius: 8px 8px 0 0; font-size: 0.85rem; font-weight: 500; color: #64748b; text-decoration: none; display: flex; align-items: center; gap: 8px; transition: all 0.15s; border: 1px solid transparent; border-bottom: none; position: relative; }
        .intel-tab:hover { color: #e2e8f0; background: rgba(255,255,255,0.03); }
        .intel-tab.active { color: #3b82f6; background: #0f1724; border-color: rgba(255,255,255,0.06); }
        .intel-tab.active::after { content: ''; position: absolute; bottom: -1px; left: 0; right: 0; height: 2px; background: #3b82f6; }
        .intel-tab i { font-size: 1rem; }

        .research-container { padding: 24px 32px; }

        .address-section { background: #1a2332; border-radius: 16px; padding: 28px; border: 1px solid rgba(255,255,255,0.06); margin-bottom: 24px; }
        .address-section h2 { font-size: 1.1rem; font-weight: 700; color: #e2e8f0; margin-bottom: 4px; }
        .address-section .subtitle { font-size: 0.85rem; color: #64748b; margin-bottom: 20px; }
        .search-form { display: flex; gap: 12px; flex-wrap: wrap; }
        .search-input { flex: 1; min-width: 200px; padding: 12px 16px; border-radius: 10px; border: 1px solid rgba(255,255,255,0.1); background: #0f1724; color: #e2e8f0; font-size: 0.95rem; outline: none; transition: border-color 0.15s; }
        .search-input:focus { border-color: #3b82f6; }
        .search-input::placeholder { color: #475569; }
        .search-select { padding: 12px 16px; border-radius: 10px; border: 1px solid rgba(255,255,255,0.1); background: #0f1724; color: #e2e8f0; font-size: 0.95rem; outline: none; min-width: 150px; }

        .pipeline-grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: 20px; margin-bottom: 24px; }
        .pipeline-card { background: #1a2332; border-radius: 14px; padding: 24px; border: 1px solid rgba(255,255,255,0.06); transition: all 0.2s; }
        .pipeline-card:hover { border-color: rgba(59,130,246,0.2); }
        .pipeline-card.active { border-color: rgba(16,185,129,0.4); }
        .pipeline-header { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 16px; }
        .pipeline-icon { width: 44px; height: 44px; border-radius: 12px; display: flex; align-items: center; justify-content: center; font-size: 1.2rem; }
        .pipeline-icon.danimal { background: rgba(16,185,129,0.12); color: #10b981; }
        .pipeline-icon.eagle { background: rgba(59,130,246,0.12); color: #3b82f6; }
        .pipeline-icon.places { background: rgba(234,179,8,0.12); color: #eab308; }
        .pipeline-icon.census { background: rgba(139,92,246,0.12); color: #8b5cf6; }
        .pipeline-icon.fdot { background: rgba(16,185,129,0.12); color: #10b981; }
        .pipeline-status { font-size: 0.7rem; font-weight: 600; padding: 3px 10px; border-radius: 100px; text-transform: uppercase; letter-spacing: 0.5px; }
        .status-ready { background: rgba(16,185,129,0.12); color: #10b981; }
        .status-phase2 { background: rgba(234,179,8,0.12); color: #eab308; }
        .status-phase3 { background: rgba(139,92,246,0.12); color: #8b5cf6; }
        .pipeline-title { font-size: 1rem; font-weight: 600; color: #e2e8f0; margin-bottom: 4px; }
        .pipeline-desc { font-size: 0.8rem; color: #64748b; line-height: 1.5; margin-bottom: 16px; }
        .pipeline-data { border-top: 1px solid rgba(255,255,255,0.04); padding-top: 16px; }
        .pipeline-data-row { display: flex; justify-content: space-between; align-items: center; padding: 6px 0; font-size: 0.8rem; }
        .pipeline-data-label { color: #64748b; }
        .pipeline-data-value { color: #94a3b8; font-weight: 500; }
        .pipeline-data-value.pending { color: #475569; font-style: italic; }
        .pipeline-data-value.success { color: #10b981; font-weight: 600; }

        .results-section { background: #1a2332; border-radius: 16px; padding: 28px; border: 1px solid rgba(255,255,255,0.06); }
        .results-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 20px; }
        .results-title { font-size: 1.1rem; font-weight: 700; color: #e2e8f0; }
        .results-empty { text-align: center; padding: 40px 20px; }
        .results-empty i { font-size: 2.5rem; color: #334155; margin-bottom: 12px; }
        .results-empty h3 { font-size: 1rem; color: #94a3b8; margin-bottom: 4px; }
        .results-empty p { font-size: 0.85rem; color: #475569; }

        .industry-list { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 12px; }
        .industry-item { display: flex; justify-content: space-between; align-items: center; padding: 12px 16px; border-radius: 10px; background: rgba(255,255,255,0.02); border: 1px solid rgba(255,255,255,0.04); }
        .industry-name { font-size: 0.85rem; color: #e2e8f0; }
        .industry-count { font-size: 0.9rem; font-weight: 600; color: #10b981; background: rgba(16,185,129,0.1); padding: 4px 12px; border-radius: 20px; }

        .loading { display: inline-block; width: 16px; height: 16px; border: 2px solid rgba(255,255,255,0.1); border-top-color: #3b82f6; border-radius: 50%; animation: spin 0.8s linear infinite; }
        @keyframes spin { to { transform: rotate(360deg); } }

        @media (max-width: 1100px) { .pipeline-grid { grid-template-columns: 1fr; } }
    </style>
</head>
<body>
    <nav class="sidebar">
        <div class="sidebar-header">
            <h2><span>ZENITH</span> <span>OS</span></h2>
            <p><%= typeof tenant !== 'undefined' && tenant ? tenant.name : 'CRE Consultants' %></p>
        </div>
        <div class="nav-section">
            <div class="nav-section-label">Navigation</div>
            <a href="/dashboard" class="nav-item"><span class="nav-icon"><i class="bi bi-house"></i></span> Dashboard</a>
            <a href="/crm" class="nav-item"><span class="nav-icon"><i class="bi bi-kanban"></i></span> CRM Pipeline</a>
            <a href="/intel" class="nav-item active"><span class="nav-icon"><i class="bi bi-broadcast"></i></span> INTEL</a>
            <a href="/huddle" class="nav-item"><span class="nav-icon"><i class="bi bi-chat-dots"></i></span> Huddle</a>
            <a href="/mailer" class="nav-item"><span class="nav-icon"><i class="bi bi-envelope"></i></span> Mailer</a>
            <a href="/vault" class="nav-item"><span class="nav-icon"><i class="bi bi-lock"></i></span> Vault</a>
            <a href="/pm" class="nav-item"><span class="nav-icon"><i class="bi bi-building"></i></span> Property Mgmt</a>
            <a href="/cfo" class="nav-item"><span class="nav-icon"><i class="bi bi-graph-up"></i></span> CFO</a>
            <a href="/danimal" class="nav-item"><span class="nav-icon"><i class="bi bi-database"></i></span> Danimal Data</a>
        </div>
        <div class="sidebar-footer">
            <div class="user-info">
                <div class="user-avatar"><%= typeof user !== 'undefined' && user && user.name ? user.name.charAt(0) : 'U' %></div>
                <div>
                    <div class="user-name"><%= typeof user !== 'undefined' && user ? user.name : 'User' %></div>
                    <div class="user-role"><%= typeof user !== 'undefined' && user ? user.role : '' %></div>
                </div>
            </div>
            <a href="/auth/logout"><i class="bi bi-box-arrow-left"></i> Sign Out</a>
        </div>
    </nav>

    <div class="main-content">
        <div class="top-bar">
            <h1><span class="accent">INTEL</span> Research</h1>
            <div class="top-bar-actions">
                <a href="/intel" class="btn btn-ghost"><i class="bi bi-arrow-left"></i> Back to INTEL</a>
            </div>
        </div>

        <div class="intel-tabs">
            <a href="/intel" class="intel-tab"><i class="bi bi-grid-3x3-gap"></i> Overview</a>
            <a href="/intel/research" class="intel-tab active"><i class="bi bi-search"></i> Research</a>
            <a href="/intel/create" class="intel-tab"><i class="bi bi-brush"></i> Create</a>
        </div>

        <div class="research-container">
            <div class="address-section">
                <h2><i class="bi bi-geo-alt"></i> Market Research</h2>
                <div class="subtitle">Search by city or county to gather business intelligence from Danimal Data's 1.5M+ Florida professional licensees.</div>
                <div class="search-form">
                    <input type="text" class="search-input" id="cityInput" placeholder="City (e.g. Fort Myers, Miami, Tampa)" />
                    <input type="text" class="search-input" id="countyInput" placeholder="County (e.g. Lee, Miami-Dade)" style="max-width: 250px;" />
                    <button class="btn btn-blue" id="researchBtn" onclick="startResearch()"><i class="bi bi-search"></i> Research</button>
                </div>
            </div>

            <div class="pipeline-grid">
                <!-- Danimal Data - READY -->
                <div class="pipeline-card" id="danimalCard">
                    <div class="pipeline-header">
                        <div class="pipeline-icon danimal"><i class="bi bi-database"></i></div>
                        <span class="pipeline-status status-ready">Ready</span>
                    </div>
                    <div class="pipeline-title">Danimal Data — Professional Licensees</div>
                    <div class="pipeline-desc">1.5M+ Florida DBPR professional licensees including real estate, contractors, cosmetologists, food service, and more.</div>
                    <div class="pipeline-data">
                        <div class="pipeline-data-row">
                            <span class="pipeline-data-label">Total Leads</span>
                            <span class="pipeline-data-value" id="danimalTotal">—</span>
                        </div>
                        <div class="pipeline-data-row">
                            <span class="pipeline-data-label">Industries</span>
                            <span class="pipeline-data-value" id="danimalIndustries">—</span>
                        </div>
                    </div>
                </div>

                <!-- Census API -->
                <div class="pipeline-card">
                    <div class="pipeline-header">
                        <div class="pipeline-icon census"><i class="bi bi-people"></i></div>
                        <span class="pipeline-status status-phase2">Phase 2</span>
                    </div>
                    <div class="pipeline-title">Census API — Demographics</div>
                    <div class="pipeline-desc">Population, median household income, home values, and employment statistics at the county level.</div>
                    <div class="pipeline-data">
                        <div class="pipeline-data-row">
                            <span class="pipeline-data-label">Population</span>
                            <span class="pipeline-data-value pending" id="censusPopulation">—</span>
                        </div>
                        <div class="pipeline-data-row">
                            <span class="pipeline-data-label">Median HH Income</span>
                            <span class="pipeline-data-value pending" id="censusIncome">—</span>
                        </div>
                    </div>
                </div>

                <!-- Google Places -->
                <div class="pipeline-card">
                    <div class="pipeline-header">
                        <div class="pipeline-icon places"><i class="bi bi-shop"></i></div>
                        <span class="pipeline-status status-phase2">Phase 2</span>
                    </div>
                    <div class="pipeline-title">Google Places — Tenant Identification</div>
                    <div class="pipeline-desc">Automatic nearby business identification. Returns tenant names, categories, ratings, and operating status.</div>
                    <div class="pipeline-data">
                        <div class="pipeline-data-row">
                            <span class="pipeline-data-label">Tenants Found</span>
                            <span class="pipeline-data-value pending">Not yet queried</span>
                        </div>
                        <div class="pipeline-data-row">
                            <span class="pipeline-data-label">Logo Matches</span>
                            <span class="pipeline-data-value pending">—</span>
                        </div>
                    </div>
                </div>

                <!-- FDOT -->
                <div class="pipeline-card">
                    <div class="pipeline-header">
                        <div class="pipeline-icon fdot"><i class="bi bi-signpost-2"></i></div>
                        <span class="pipeline-status status-phase2">Phase 2</span>
                    </div>
                    <div class="pipeline-title">FDOT — Traffic Counts (VPD)</div>
                    <div class="pipeline-desc">Florida DOT annual traffic counting data. Standard for CRE location analysis.</div>
                    <div class="pipeline-data">
                        <div class="pipeline-data-row">
                            <span class="pipeline-data-label">Nearest Station</span>
                            <span class="pipeline-data-value pending">—</span>
                        </div>
                        <div class="pipeline-data-row">
                            <span class="pipeline-data-label">AADT (Vehicles/Day)</span>
                            <span class="pipeline-data-value pending">—</span>
                        </div>
                    </div>
                </div>
            </div>

            <div class="results-section">
                <div class="results-header">
                    <div class="results-title" id="resultsTitle">Research Results</div>
                    <button class="btn btn-primary" id="createProjectBtn" style="display: none;" onclick="createProject()">
                        <i class="bi bi-plus-lg"></i> Create INTEL Project
                    </button>
                </div>
                <div id="research-results">
                    <div class="results-empty">
                        <i class="bi bi-search"></i>
                        <h3>Enter a City or County</h3>
                        <p>Search above to see professional licensee breakdown by industry.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        let lastSearchCity = '';
        let lastSearchCounty = '';

        async function startResearch() {
            const city = document.getElementById('cityInput').value.trim();
            const county = document.getElementById('countyInput').value.trim();
            
            if (!city && !county) {
                document.getElementById('cityInput').focus();
                return;
            }

            lastSearchCity = city;
            lastSearchCounty = county;

            const btn = document.getElementById('researchBtn');
            btn.innerHTML = '<span class="loading"></span> Researching...';
            btn.disabled = true;

            // Reset displays
            document.getElementById('danimalTotal').textContent = '...';
            document.getElementById('danimalIndustries').textContent = '...';
            document.getElementById('danimalCard').classList.remove('active');

            try {
                const response = await fetch('/api/intel/research', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ city, county })
                });

                const data = await response.json();

                if (data.success && data.results) {
                    displayResults(data.results);
                } else {
                    showError(data.error || 'Research failed');
                }
            } catch (err) {
                showError('Network error: ' + err.message);
            }

            btn.innerHTML = '<i class="bi bi-search"></i> Research';
            btn.disabled = false;
        }

        function displayResults(results) {
            const danimal = results.danimalData;
            
            if (danimal && danimal.total > 0) {
                document.getElementById('danimalTotal').textContent = danimal.total.toLocaleString();
                document.getElementById('danimalTotal').classList.add('success');
                document.getElementById('danimalIndustries').textContent = danimal.byIndustry.length + ' categories';
                document.getElementById('danimalIndustries').classList.add('success');
                document.getElementById('danimalCard').classList.add('active');

                // Show industry breakdown
                const resultsDiv = document.getElementById('research-results');
                const searchLabel = lastSearchCity || lastSearchCounty || 'Selected Area';
                
                let html = '<div class="industry-list">';
                danimal.byIndustry.forEach(item => {
                    html += '<div class="industry-item">';
                    html += '<span class="industry-name">' + (item.industry || 'Other') + '</span>';
                    html += '<span class="industry-count">' + parseInt(item.count).toLocaleString() + '</span>';
                    html += '</div>';
                });
                html += '</div>';

                resultsDiv.innerHTML = html;
                document.getElementById('resultsTitle').textContent = 'Professional Licensees in ' + searchLabel;
                document.getElementById('createProjectBtn').style.display = 'inline-flex';
            } else {
                document.getElementById('danimalTotal').textContent = '0';
                document.getElementById('danimalIndustries').textContent = 'None found';
                document.getElementById('research-results').innerHTML = 
                    '<div class="results-empty"><i class="bi bi-inbox"></i><h3>No Results</h3><p>No professional licensees found for this search.</p></div>';
            }
        }

        function showError(message) {
            document.getElementById('research-results').innerHTML = 
                '<div class="results-empty"><i class="bi bi-exclamation-triangle"></i><h3>Error</h3><p>' + message + '</p></div>';
        }

        function createProject() {
            const search = lastSearchCity || lastSearchCounty;
            window.location.href = '/intel/create?address=' + encodeURIComponent(search);
        }

        // Enter key triggers research
        document.getElementById('cityInput').addEventListener('keydown', (e) => { if (e.key === 'Enter') startResearch(); });
        document.getElementById('countyInput').addEventListener('keydown', (e) => { if (e.key === 'Enter') startResearch(); });
    </script>
</body>
</html>